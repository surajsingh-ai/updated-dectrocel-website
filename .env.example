PGHOST=localhost
PGPORT=5432
PGDATABASE=website
PGUSER=postgres
PGPASSWORD=your_postgres_password

# Or use one URL instead of the PG* values above:
# DATABASE_URL=postgresql://postgres:your_postgres_password@localhost:5432/website
